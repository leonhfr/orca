version: "2"

linters:
  default: none
  enable:
    - contextcheck
    - errcheck
    - errname
    - errorlint
    - exptostd
    - godot
    - gosec
    - govet
    - iface
    - ineffassign
    - makezero
    - modernize
    - nilerr
    - nolintlint
    - paralleltest
    - perfsprint
    - prealloc
    - revive
    - staticcheck
    - testifylint
    - thelper
    - unconvert
    - unparam
    - unused
  settings:
    gosec:
      excludes:
        - G104
        - G115
        - G602
    govet:
      enable-all: true
  exclusions:
    generated: lax
    rules:
      - linters:
          - govet
        path: (.+)_test.go

formatters:
  enable:
    - gci
  settings:
    gci:
      sections:
        - standard
        - default
        - prefix(github.com/leonhfr/orca)
  exclusions:
    generated: lax
